.nested-fields
  = f.inputs :name => 'Ingredient' do
    = f.input :pizza_id, :as => :hidden, :input_html => { :value => @pizza.id }
    = f.input :ingredient
    = f.input :quantity
    = link_to_remove_association "Remove this ingredient", f, :class => 'btn btn-danger'
